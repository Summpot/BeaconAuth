//! Legacy file retained to avoid churn in git history.
//!
//! The Cloudflare Worker implementation no longer uses the old database-specific layer that
//! previously lived here. The active worker database helpers live in `wasm/db/mod.rs`.
//!
//! This file is intentionally empty.

