---
title: API 参考
description: BeaconAuth 暴露的 HTTP 端点。
---

## 基础路径

API 可通过以下路径访问：

- `/v1/*`（直接访问）
- `/api/v1/*`（挂载在 `/api` 下时）

## 核心端点

### 会话

- `POST /v1/login`
- `POST /v1/register`
- `POST /v1/refresh`
- `POST /v1/logout`
- `GET /v1/user/me`
- `GET /v1/user/me/avatar`
- `POST /v1/user/change-password`
- `POST /v1/user/change-username`
- `POST /v1/user/profile`

示例：`POST /v1/login`

```json
{
  "username": "player123",
  "password": "secure_password"
}
```

### Minecraft 认证

- `POST /v1/minecraft-jwt`

示例请求：

```json
{
  "challenge": "PKCE_challenge_string",
  "redirect_port": 38125
}
```

### OAuth

- `POST /v1/oauth/start`
- `POST /v1/oauth/link/start`
- `GET /v1/oauth/callback`

### Passkeys（WebAuthn）

- `POST /v1/passkey/register/start`
- `POST /v1/passkey/register/finish`
- `POST /v1/passkey/auth/start`
- `POST /v1/passkey/auth/finish`
- `POST /v1/passkey/delete`
- `GET /v1/passkey/list`
- `DELETE /v1/passkey/{id}`

### 身份

- `GET /v1/identities`
- `DELETE /v1/identities/{id}`

### 配置与 JWKS

- `GET /v1/config`
- `GET /.well-known/jwks.json`

### 管理

- `POST /v1/admin/migrations/up`

## 说明

- 会话端点会设置 HttpOnly 的访问/刷新 Cookie。
- JWT 使用 ES256 签名，并通过 JWKS 发布。
- 部分端点需要已认证会话。
