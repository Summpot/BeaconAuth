---
title: API 參考
description: BeaconAuth 提供的 HTTP 端點。
---

## Base paths

API 提供於:

- `/v1/*`（直接）
- `/api/v1/*`（掛載在 `/api` 下時）

## 核心端點

### Sessions

- `POST /v1/login`
- `POST /v1/register`
- `POST /v1/refresh`
- `POST /v1/logout`
- `GET /v1/user/me`
- `GET /v1/user/me/avatar`
- `POST /v1/user/change-password`
- `POST /v1/user/change-username`
- `POST /v1/user/profile`

範例：`POST /v1/login`

```json
{
  "username": "player123",
  "password": "secure_password"
}
```

### Minecraft 驗證

- `POST /v1/minecraft-jwt`

範例 request:

```json
{
  "challenge": "PKCE_challenge_string",
  "redirect_port": 38125
}
```

### OAuth

- `POST /v1/oauth/start`
- `POST /v1/oauth/link/start`
- `GET /v1/oauth/callback`

### Passkeys（WebAuthn）

- `POST /v1/passkey/register/start`
- `POST /v1/passkey/register/finish`
- `POST /v1/passkey/auth/start`
- `POST /v1/passkey/auth/finish`
- `POST /v1/passkey/delete`
- `GET /v1/passkey/list`
- `DELETE /v1/passkey/{id}`

### Identities

- `GET /v1/identities`
- `DELETE /v1/identities/{id}`

### 設定與 JWKS

- `GET /v1/config`
- `GET /.well-known/jwks.json`

### Admin

- `POST /v1/admin/migrations/up`

## Notes

- Session 端點會以 HttpOnly cookies 設定 access/refresh tokens。
- JWT 以 ES256 簽章並透過 JWKS 發佈。
- 部分端點需要已驗證的 session。
