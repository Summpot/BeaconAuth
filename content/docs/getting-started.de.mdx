---
title: Loslegen
description: Schnelle Wege, BeaconAuth lokal oder in Produktion zu betreiben.
---

## Docker verwenden (empfohlen)

```bash
# Aktuelles Image ziehen
docker pull ghcr.io/summpot/beacon_auth:latest

# Server starten
docker run -d --name beaconauth \
  -p 8080:8080 \
  -v $(pwd)/data:/app/data \
  -e DATABASE_URL=sqlite:///app/data/beacon_auth.db \
  ghcr.io/summpot/beacon_auth:latest
```

Der Server ist unter `http://localhost:8080` erreichbar.

## Vorgebaute Binärdateien verwenden

Lade das Serverarchiv für dein Betriebssystem und deine CPU von der offiziellen Projekt-Release-Seite herunter und entpacke es lokal.

**Linux / macOS**:

```bash
tar -xzf beaconauth-<platform>.tar.gz
chmod +x beacon
./beacon serve
```

**Windows (amd64)**:

```powershell
# ZIP herunterladen und entpacken
# In PowerShell ausführen:
.\beacon.exe serve
```

## Aus dem Quellcode bauen

### Voraussetzungen

- Rust (stable)
- Node.js (LTS)
- pnpm

### Build-Schritte

```bash
# Repository klonen
git clone https://github.com/Summpot/beacon_auth.git
cd beacon_auth

# Frontend-Abhängigkeiten installieren
pnpm install

# Rust-Workspace bauen
cargo build --workspace --release
```

Die Binärdatei liegt unter `target/release/beacon`.
