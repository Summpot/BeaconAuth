---
title: 서버 배포
description: BeaconAuth를 독립 실행형 서버로 운영하는 방법.
---

## 로컬 실행

```bash
beacon serve
```

환경 변수 또는 CLI 플래그로 설정합니다. 자세한 내용은 [구성](./configuration) 페이지를 참고하세요.

## 데이터베이스 설정

CLI로 마이그레이션을 실행하고 사용자를 관리할 수 있습니다:

```bash
beacon migrate --database-url sqlite://./beacon_auth.db
beacon create-user --username admin --password your_secure_password
beacon list-users
beacon delete-user --username admin
```

## Docker Compose (프로덕션)

```yaml
version: "3.8"

services:
  beaconauth:
    image: ghcr.io/summpot/beacon_auth:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: sqlite:///app/data/beacon_auth.db
      BIND_ADDRESS: 0.0.0.0:8080
      JWT_EXPIRATION: 7200
      BASE_URL: https://auth.example.com
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      MICROSOFT_CLIENT_ID: ${MICROSOFT_CLIENT_ID}
      MICROSOFT_CLIENT_SECRET: ${MICROSOFT_CLIENT_SECRET}
      MICROSOFT_TENANT: ${MICROSOFT_TENANT}
    restart: unless-stopped
```

서비스를 시작합니다:

```bash
docker-compose up -d
```

## 리버스 프록시 (Nginx)

```nginx
server {
    listen 80;
    server_name auth.example.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

HTTPS는 Let's Encrypt를 사용할 수 있습니다:

```bash
certbot --nginx -d auth.example.com
```
