---
title: API 참조
description: BeaconAuth가 제공하는 HTTP 엔드포인트.
---

## 기본 경로

API는 다음 경로로 제공됩니다:

- `/v1/*` (직접)
- `/api/v1/*` (`/api` 아래에 마운트된 경우)

## 핵심 엔드포인트

### 세션

- `POST /v1/login`
- `POST /v1/register`
- `POST /v1/refresh`
- `POST /v1/logout`
- `GET /v1/user/me`
- `GET /v1/user/me/avatar`
- `POST /v1/user/change-password`
- `POST /v1/user/change-username`
- `POST /v1/user/profile`

예시: `POST /v1/login`

```json
{
  "username": "player123",
  "password": "secure_password"
}
```

### Minecraft 인증

- `POST /v1/minecraft-jwt`

예시 요청:

```json
{
  "challenge": "PKCE_challenge_string",
  "redirect_port": 38125
}
```

### OAuth

- `POST /v1/oauth/start`
- `POST /v1/oauth/link/start`
- `GET /v1/oauth/callback`

### 패스키 (WebAuthn)

- `POST /v1/passkey/register/start`
- `POST /v1/passkey/register/finish`
- `POST /v1/passkey/auth/start`
- `POST /v1/passkey/auth/finish`
- `POST /v1/passkey/delete`
- `GET /v1/passkey/list`
- `DELETE /v1/passkey/{id}`

### 아이덴티티

- `GET /v1/identities`
- `DELETE /v1/identities/{id}`

### 구성 및 JWKS

- `GET /v1/config`
- `GET /.well-known/jwks.json`

### 관리자

- `POST /v1/admin/migrations/up`

## 참고

- 세션 엔드포인트는 access/refresh 토큰을 위한 HttpOnly 쿠키를 설정합니다.
- JWT는 ES256으로 서명되며 JWKS로 공개됩니다.
- 일부 엔드포인트는 인증된 세션이 필요합니다.
